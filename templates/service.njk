{% extends "_base.njk" %} 
{% set service = row %}

{% block content %}
<div class="container">
  <h1>{{service.name}}</h1>
  <div class="row g-5">
    <div class="col-md-4">
      {% if service.header %}
        {% markdown %}
        {{service.header}}
        {% endmarkdown %}
      {% endif %}
      {% if service.images %}
        {% for image in service.images %}
        <img
        class="img-fluid img-full"
        src="{{image.thumbnails.large.url}}"
        loading="lazy"
        />
        <br />
        <br />
        {% endfor %}
      {% endif %}
    </div>
    <div class="col-md-8">
      {% if service.thumb %}
      <img
        class="img-fluid img-full"
        src="{{service.thumb.thumbnails.full.url}}"
        loading="lazy"
      />
      {% endif %}
      <br />
      {% if service.text %}
        {% markdown %}
        {{service.text}}
        {% endmarkdown %}
      {% endif %}
    </div>
  </div>
  
  <!--<p>
    {% for target in service.targets %}
    <span>{{target}}</span>{{ "," if not loop.last }}
    {% endfor %}
  </p>-->
</div>
{% endblock %}